import React from 'react';
import MenuGrid from '../MenuGrid/MenuGrid';

const Menu = (props) => {
        return (
            <div>
                <section className="section-menu">
                    <MenuGrid />
                </section>
            </div>
        );
}

export default Menu;